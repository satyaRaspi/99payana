Payana Screening Registration v1.2.41 - Clean Railway Build

This package uses a Python-only Dockerfile.
Railway should NOT show:
- frontend-build
- npm install
- npm ci
- vite build

The frontend is already built and committed inside:
frontend/dist/

Railway should only run:
- pip install -r backend/requirements.txt
- uvicorn backend.main:app

If Railway still shows frontend-build, GitHub still has an old Dockerfile.
